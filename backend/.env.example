# NestJS / API
PORT=3000
JWT_SECRET=<choose-your-jwt-signature-secret>

# CLI tools (migration:run, db:setup) — used by typeorm.datasource.ts when running outside Docker
# POSTGRES_CLI_HOST defaults to localhost; only set this if your DB is on a different host
POSTGRES_CLI_HOST=localhost